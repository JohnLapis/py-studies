from sieve_of_erastosthenes import sieve
from isprime import isprime

def main(limit):
    n = 0
    for i in sieve(limit):
        if isprime(int(str(i)[-1::-1])):
            n += 1

    return n


if __name__ == '__main__':
    print(main(1000000))
